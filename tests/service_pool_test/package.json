{
  "name": "service_pool_test",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "tsc --build .",
    "clean:build": "npm run clean && npm run build",
    "start": "node --expose-gc .",
    "test": "npm run clean:build && node --expose-gc .",
    "monitor": "nodemon ."
  },
  "author": "FAR",
  "license": "MIT",
  "dependencies": {
    "network-services": "file:../.."
  },
  "type": "module",
  "engines": {
    "npm": ">=10.2.3",
    "node": ">=18.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "typescript": "^5.9.3"
  }
}
